[package]
name = "seqpool"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
crossbeam-utils = { version = "0.8", default-features = false }

[features]
default = []
metrics = []

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
snmalloc-rs = "0.7"

[[bench]]
name = "pool"
harness = false
