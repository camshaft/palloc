[package]
name = "seqpool"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
crossbeam-queue = "0.3"

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }
tikv-jemallocator = "0.6"
mimalloc = "0.1"

[[bench]]
name = "pool"
harness = false
